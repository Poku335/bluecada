<%= form_with(model: postneo_staging) do |form| %>
  <% if postneo_staging.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(postneo_staging.errors.count, "error") %> prohibited this postneo_staging from being saved:</h2>

      <ul>
        <% postneo_staging.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :code, style: "display: block" %>
    <%= form.text_field :code %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
