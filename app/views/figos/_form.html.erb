<%= form_with(model: figo) do |form| %>
  <% if figo.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(figo.errors.count, "error") %> prohibited this figo from being saved:</h2>

      <ul>
        <% figo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :code, style: "display: block" %>
    <%= form.text_field :code %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
