<%= form_with(model: type_stage) do |form| %>
  <% if type_stage.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(type_stage.errors.count, "error") %> prohibited this type_stage from being saved:</h2>

      <ul>
        <% type_stage.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :code, style: "display: block" %>
    <%= form.text_field :code %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
